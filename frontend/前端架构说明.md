# 前端架构说明文档

## 📌 目录
1. [前端入口](#前端入口)
2. [路由机制](#路由机制)
3. [样式修改位置](#样式修改位置)
4. [JS修改位置](#js修改位置)
5. [文件加载顺序](#文件加载顺序)
6. [新功能开发流程](#新功能开发流程)

---

## 🚪 前端入口

### **主入口文件：`index.html`**

```
C:\Users\Administrator\Desktop\six666\frontend\index.html
```

这是整个前端的入口页面，包含：
- 所有的HTML结构
- CSS样式的引入
- JavaScript模块的引入
- 侧边栏菜单定义

**关键结构：**
```html
<!DOCTYPE html>
<html>
<head>
  <!-- CSS文件（按加载顺序） -->
  <link rel="stylesheet" href="css/variables.css">
  <link rel="stylesheet" href="css/layout.css">
  <link rel="stylesheet" href="css/components.css">
  <link rel="stylesheet" href="css/pages.css">
  <link rel="stylesheet" href="css/responsive.css">
  <link rel="stylesheet" href="css/scheduler_styles.css">
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <div class="layout">
    <!-- 侧边栏：所有菜单按钮 -->
    <aside class="sidebar">
      <button id="menuCollectBtn">📥 采集</button>
      <button id="menuRecordsBtn">📋 开奖结果</button>
      <button id="menuHot20Minus10Btn">去10的最热20分析</button>
      <!-- ...更多按钮... -->
    </aside>

    <!-- 主内容区：所有页面容器 -->
    <main class="main-content">
      <div id="collectPage">采集页面</div>
      <div id="recordsPage">开奖结果页面</div>
      <div id="hot20Minus10Page">去10最热20分析页面</div>
      <!-- ...更多页面... -->
    </main>
  </div>

  <!-- JavaScript文件（按加载顺序） -->
  <script src="js/config.js"></script>
  <script src="js/utils.js"></script>
  <script src="js/api.js"></script>
  <script src="js/pages.js"></script>
  <script src="js/modules/collect.js"></script>
  <script src="js/features/hot20Analysis.js"></script>
  <!-- ...更多模块... -->
  <script src="js/main.js"></script> <!-- 最后加载 -->
</body>
</html>
```

---

## 🧭 路由机制

### **路由核心文件：`js/pages.js`**

前端采用的是**单页应用（SPA）模式**，通过 JavaScript 控制页面显示/隐藏。

### **路由配置表**

```javascript
// 位置：js/pages.js 第7-43行
const PAGE_CONFIG = {
  // 按钮ID                 页面ID                    页面标题
  'menuCollectBtn':        { pageId: 'collectPage',         title: '数据采集' },
  'menuRecordsBtn':        { pageId: 'recordsPage',         title: '开奖结果' },
  'menuHot20Minus10Btn':   { pageId: 'hot20Minus10Page',    title: '去10的最热20分析' },
  'menuRecommendBtn':      { pageId: 'recommendPage',       title: '推荐8码' },
  // ...更多映射...
};
```

### **路由流程图**

```
用户点击侧边栏按钮
    ↓
触发按钮的 click 事件 (pages.js:394-427行)
    ↓
根据按钮ID查找 PAGE_CONFIG
    ↓
调用 showOnlyPage(pageId) (pages.js:49-374行)
    ↓
1. 隐藏所有页面 (display: none)
2. 显示目标页面 (display: block)
3. 调用页面的初始化函数（如果需要）
    ↓
更新页面标题 updatePageTitle() (pages.js:380-385行)
```

### **路由核心函数**

#### **1. `showOnlyPage(pageId)` - 页面切换**
```javascript
// 位置：js/pages.js:49-374
function showOnlyPage(pageId) {
  // 1. 隐藏所有页面
  document.querySelectorAll('[id$="Page"]').forEach(page => {
    page.style.display = 'none';
  });

  // 2. 显示目标页面
  const targetPage = document.getElementById(pageId);
  targetPage.style.display = 'block';

  // 3. 如果页面需要初始化，调用对应的 init 函数
  if (pageId === 'hot20Minus10Page') {
    initHot20Minus10Page(); // 调用 hot20Analysis.js 中的初始化函数
  }
}
```

#### **2. `initSidebarMenu()` - 菜单初始化**
```javascript
// 位置：js/pages.js:390-548
function initSidebarMenu() {
  // 遍历所有按钮，绑定点击事件
  Object.keys(PAGE_CONFIG).forEach(buttonId => {
    const btn = document.getElementById(buttonId);
    btn.addEventListener('click', function() {
      const config = PAGE_CONFIG[buttonId];
      showOnlyPage(config.pageId);
      updatePageTitle(config.title);
    });
  });
}
```

---

## 🎨 样式修改位置

### **CSS文件结构**

```
frontend/css/
├── variables.css        ← 全局变量（颜色、字体、间距等）
├── layout.css           ← 布局样式（sidebar、main-content）
├── components.css       ← 组件样式（按钮、表格、卡片等）
├── pages.css            ← 页面样式（各个功能页面的专属样式）
├── responsive.css       ← 响应式样式（移动端适配）
├── scheduler_styles.css ← 定时任务样式
└── style.css            ← 兼容旧样式（逐步迁移中）
```

### **样式修改指南**

| 要修改的内容 | 修改文件 | 说明 |
|------------|---------|------|
| **颜色、字体** | `css/variables.css` | 定义CSS变量，全局生效 |
| **侧边栏样式** | `css/layout.css` | `.sidebar` 相关样式 |
| **按钮样式** | `css/components.css` | `.menu-btn`、`.btn-primary` 等 |
| **表格样式** | `css/components.css` | `.data-table` 相关样式 |
| **页面布局** | `css/layout.css` | `.layout`、`.main-content` 等 |
| **特定页面样式** | `css/pages.css` | 例如 `#hot20Minus10Page` |
| **移动端适配** | `css/responsive.css` | 媒体查询 `@media` |

### **样式修改示例**

#### **示例1：修改按钮颜色**
```css
/* 文件：css/components.css */
.menu-btn {
  background: #2980d9;  /* 修改这里改变按钮背景色 */
  color: #fff;          /* 修改这里改变按钮文字颜色 */
  padding: 10px 20px;
}

.menu-btn:hover {
  background: #3498db;  /* 修改这里改变按钮悬停效果 */
}
```

#### **示例2：修改表格样式**
```css
/* 文件：css/components.css */
.data-table thead {
  background: linear-gradient(135deg, #2980d9, #3498db); /* 表头渐变色 */
  color: #fff;
}

.data-table tbody tr:nth-child(even) {
  background: #f8f9fa; /* 偶数行背景色 */
}
```

#### **示例3：修改特定页面样式**
```css
/* 文件：css/pages.css */
#hot20Minus10Page {
  padding: 20px;
  background: #fff;
}

#hot20Minus10Page h2 {
  color: #2980d9;
  font-size: 24px;
}
```

---

## 💻 JS修改位置

### **JavaScript文件结构**

```
frontend/js/
├── core/                     ← 核心模块（状态管理、错误处理等）
│   ├── state.js
│   ├── errorHandler.js
│   └── ui.js
│
├── utils/                    ← 工具函数（年份过滤等）
│   └── yearFilter.js
│
├── modules/                  ← 基础功能模块
│   ├── collect.js            ← 数据采集
│   ├── records.js            ← 开奖记录
│   ├── recommend.js          ← 推荐8码
│   ├── tens-analysis.js      ← 第N位十位分析
│   ├── units-analysis.js     ← 第N个码个位分析
│   ├── range-analysis.js     ← +1~+20区间分析
│   ├── minus-range-analysis.js ← -1~-20区间分析
│   ├── plus-minus6-analysis.js ← 加减前6码分析
│   ├── place-management.js   ← 关注点登记
│   ├── place-results.js      ← 关注点登记结果
│   ├── place-analysis.js     ← 关注点分析
│   ├── favorite-numbers.js   ← 关注号码管理
│   ├── xiao-analysis.js      ← 肖分析
│   ├── color-analysis.js     ← 波色分析
│   ├── recommend-hit.js      ← 推荐8码命中情况
│   ├── betting.js            ← 投注登记
│   └── bet-report.js         ← 投注点报表
│
├── features/                 ← 高级功能模块
│   ├── hot20Analysis.js      ← 去10的最热20分析 ⭐
│   ├── recommend16.js        ← 推荐16码
│   ├── fivePeriodThreexiao.js ← 五期三肖
│   ├── seventhSmart20.js     ← 第7码智能推荐20码
│   ├── seventhRangeAnalysis.js ← 第7码区间分析
│   ├── front6Szz.js          ← 前6码三中三
│   ├── plusMinus6.js         ← 加减前6码
│   ├── eachIssue.js          ← 每期分析
│   ├── twoGroups.js          ← 2组观察分析
│   ├── numberGapAnalysis.js  ← 号码间隔期数分析
│   ├── numberMissingAnalysis.js ← 查询遗漏期数开奖
│   └── maxMissAlert.js       ← 遗漏提醒
│
├── config.js                 ← 配置文件（后端地址）
├── utils.js                  ← 通用工具函数
├── api.js                    ← API请求封装
├── pages.js                  ← 页面路由管理 ⭐
└── main.js                   ← 主入口 ⭐
```

### **JS修改指南**

| 要修改的内容 | 修改文件 | 说明 |
|------------|---------|------|
| **后端地址** | `js/config.js` | `window.BACKEND_URL` |
| **添加新页面** | `js/pages.js` | 添加到 `PAGE_CONFIG` |
| **API请求** | `js/api.js` | 封装的API函数 |
| **通用工具** | `js/utils.js` | 日期、颜色、CSV导出等 |
| **去10最热20分析** | `js/features/hot20Analysis.js` | ⭐ 您要改的文件 |
| **推荐8码** | `js/modules/recommend.js` | 推荐8码功能 |
| **开奖记录** | `js/modules/records.js` | 开奖记录查询 |

### **JS修改示例**

#### **示例1：修改"去10的最热20分析"功能**
```javascript
// 文件：js/features/hot20Analysis.js

// 1. 修改初始化函数
function initHot20Minus10Page() {
  // 您的修改代码
}

// 2. 修改数据加载函数
async function loadHot20Analysis(type, pos, page, year) {
  // 构建请求URL
  let url = `${window.BACKEND_URL}/api/hot20_minus10?...`;

  // 发起请求
  const response = await fetch(url);
  const data = await response.json();

  // 渲染结果
  renderHot20Result(data);
}

// 3. 修改渲染函数
function renderHot20Result(data) {
  // 修改表格HTML
  let html = `...`;
}
```

#### **示例2：添加新的API请求**
```javascript
// 文件：js/api.js

// 添加新的API函数
async function getHot20Data(lottery_type, pos) {
  const url = `${window.BACKEND_URL}/api/hot20_minus10?lottery_type=${lottery_type}&pos=${pos}`;
  const response = await fetch(url);
  return await response.json();
}

// 在其他模块中使用
// const data = await getHot20Data('am', 7);
```

---

## 📚 文件加载顺序

前端文件按照以下顺序加载（位于 `index.html` 底部）：

```html
<!-- 1️⃣ 核心工具模块 -->
<script src="js/core/state.js"></script>
<script src="js/core/errorHandler.js"></script>
<script src="js/core/ui.js"></script>
<script src="js/core/common.js"></script>

<!-- 2️⃣ 基础模块 -->
<script src="js/config.js"></script>
<script src="js/utils.js"></script>
<script src="js/utils/yearFilter.js"></script>
<script src="js/api.js"></script>
<script src="js/pages.js"></script>       ← 路由管理

<!-- 3️⃣ 功能模块（modules/） -->
<script src="js/modules/collect.js"></script>
<script src="js/modules/records.js"></script>
<script src="js/modules/recommend.js"></script>
<!-- ...更多模块... -->

<!-- 4️⃣ 高级功能（features/） -->
<script src="js/features/recommend16.js"></script>
<script src="js/features/hot20Analysis.js"></script> ← 去10最热20分析
<script src="js/features/seventhSmart20.js"></script>
<!-- ...更多功能... -->

<!-- 5️⃣ 主入口（最后加载） -->
<script src="js/main.js"></script>        ← 主入口
```

**⚠️ 重要规则：**
- `config.js` 必须最先加载（定义 `window.BACKEND_URL`）
- `pages.js` 必须在功能模块之前加载（提供路由函数）
- `main.js` 必须最后加载（初始化应用）

---

## 🚀 新功能开发流程

### **完整流程（以"去10的最热20分析"为例）**

#### **第1步：在 `index.html` 中添加侧边栏按钮**
```html
<!-- 位置：index.html 第49行 -->
<button class="menu-btn menu-btn-sub" id="menuHot20Minus10Btn">去10的最热20分析</button>
```

#### **第2步：在 `index.html` 中添加页面容器**
```html
<!-- 位置：index.html 主内容区 -->
<div id="hot20Minus10Page" style="display:none;">
  <!-- 页面内容会由 JS 动态生成 -->
</div>
```

#### **第3步：在 `js/pages.js` 中注册路由**
```javascript
// 位置：js/pages.js 第20行
const PAGE_CONFIG = {
  'menuHot20Minus10Btn': { pageId: 'hot20Minus10Page', title: '去10的最热20分析' },
  // ...其他配置...
};
```

#### **第4步：在 `js/pages.js` 中添加初始化逻辑**
```javascript
// 位置：js/pages.js 第340-348行
if (pageId === 'hot20Minus10Page') {
  console.log('[页面管理] 检测到去10的最热20分析页面');
  if (typeof window.initHot20Minus10Page === 'function') {
    console.log('[页面管理] 调用 initHot20Minus10Page()');
    window.initHot20Minus10Page();
  }
}
```

#### **第5步：创建功能模块 JS 文件**
```javascript
// 文件：js/features/hot20Analysis.js

/**
 * 初始化页面
 */
function initHot20Minus10Page() {
  // 动态创建页面HTML
  const pageDiv = document.getElementById('hot20Minus10Page');
  pageDiv.innerHTML = `
    <h2>去10的最热20分析</h2>
    <button id="hot20QueryBtn">查询</button>
    <div id="hot20Result"></div>
  `;

  // 绑定事件
  document.getElementById('hot20QueryBtn').onclick = loadHot20Analysis;
}

/**
 * 加载数据
 */
async function loadHot20Analysis() {
  const response = await fetch(`${window.BACKEND_URL}/api/hot20_minus10`);
  const data = await response.json();
  renderHot20Result(data);
}

/**
 * 渲染结果
 */
function renderHot20Result(data) {
  const resultDiv = document.getElementById('hot20Result');
  resultDiv.innerHTML = `<table>...</table>`;
}

// 导出到全局
window.initHot20Minus10Page = initHot20Minus10Page;
```

#### **第6步：在 `index.html` 中引入 JS 文件**
```html
<!-- 位置：index.html 底部 -->
<script src="js/features/hot20Analysis.js"></script>
```

#### **第7步：添加样式（可选）**
```css
/* 文件：css/pages.css */
#hot20Minus10Page {
  padding: 20px;
}

#hot20Minus10Page h2 {
  color: #2980d9;
  margin-bottom: 20px;
}
```

---

## 🔍 调试技巧

### **1. 查看路由是否注册成功**
```javascript
// 打开浏览器控制台，输入：
console.log(PAGE_CONFIG);
// 应该能看到 'menuHot20Minus10Btn' 的配置
```

### **2. 查看初始化函数是否存在**
```javascript
// 打开浏览器控制台，输入：
console.log(typeof window.initHot20Minus10Page);
// 应该输出 "function"
```

### **3. 手动切换页面**
```javascript
// 打开浏览器控制台，输入：
showOnlyPage('hot20Minus10Page');
```

### **4. 查看当前显示的页面**
```javascript
// 打开浏览器控制台，输入：
document.querySelectorAll('[id$="Page"]').forEach(page => {
  if (page.style.display !== 'none') {
    console.log('当前页面:', page.id);
  }
});
```

---

## 📝 快速参考

### **常用文件路径**

| 用途 | 文件路径 |
|------|---------|
| 主入口 | `frontend/index.html` |
| 路由管理 | `frontend/js/pages.js` |
| 配置文件 | `frontend/js/config.js` |
| API封装 | `frontend/js/api.js` |
| 工具函数 | `frontend/js/utils.js` |
| 去10最热20分析 | `frontend/js/features/hot20Analysis.js` |
| 全局样式 | `frontend/css/style.css` |
| 组件样式 | `frontend/css/components.css` |

### **常用操作**

| 操作 | 位置 |
|------|-----|
| 添加侧边栏按钮 | `index.html` 第19-102行 |
| 注册路由 | `js/pages.js` 第7-43行 |
| 添加页面初始化逻辑 | `js/pages.js` 第49-374行 |
| 修改按钮样式 | `css/components.css` `.menu-btn` |
| 修改表格样式 | `css/components.css` `.data-table` |

---

## 🎯 总结

1. **前端入口**：`index.html`
2. **路由管理**：`js/pages.js` （核心！）
3. **样式修改**：`css/` 目录下的文件
4. **JS修改**：`js/modules/` 或 `js/features/` 目录
5. **新功能开发**：遵循7步流程

**关键点：**
- 采用单页应用（SPA）模式
- 通过 `showOnlyPage()` 控制页面切换
- 所有页面共享一个 HTML 文件
- 模块化设计，每个功能独立一个 JS 文件

---

**文档版本**：v1.0
**更新日期**：2025-01-21
**维护者**：Claude Code
